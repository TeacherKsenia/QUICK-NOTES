/*
  Optional override for the public GitHub repository that stores Windows Releases.
  Leave githubOwner/githubRepo blank when QUICK NOTES is hosted as a normal
  GitHub Pages project (https://OWNER.github.io/REPO/) — the web app detects it.

  For local testing before the repository exists, you may fill these two values.
*/
window.QuickNotesReleaseConfig = Object.freeze({
  githubOwner: '',
  githubRepo: '',
  installerAsset: 'Quick-Notes-Setup.exe'
});
